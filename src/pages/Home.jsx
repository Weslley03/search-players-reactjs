import { CardPlayer } from "../components/CardPlayer";
import { TeamQueue } from "../components/teamQueue";

const Home = () => {
  return (
    <section>
      <TeamQueue />
      <CardPlayer />
    </section>
  )
};

export default Home;
